<?xml version="1.0" encoding="utf-8" ?>
<odoo>

<record id="sale_order_line_reassign_wiz" model="ir.ui.view">
    <field name="name">sale.order.line.reasign.wiz</field>
    <field name="model">sale.order.line.reassign.wiz</field>
    <field name="arch" type="xml">
        <form string="Sale Order Lines">
            <group>
                <field name="partner_id" invisible="1" />
                <field
                        name="sale_order_id"
                        domain="[('partner_id', '=', partner_id), ('state', 'not in', ['done', 'cancel']), ('invoice_status', '!=', 'invoiced')]"
                        context="{'default_partner_id': partner_id}"
                    />
            </group>
            <footer>
                <button
                        string="Apply"
                        class="oe_highlight"
                        name="action_apply"
                        type="object"
                    />
                <button string="Close" class="oe_link" special="cancel" />
            </footer>
        </form>
    </field>
</record>

<act_window
        id="action_sale_order_line_reassign_wiz"
        name="Reassign Lines to Other Sale"
        res_model="sale.order.line.reassign.wiz"
        binding_model="sale.order.line"
        view_mode="form"
        target="new"
    />

</odoo>
